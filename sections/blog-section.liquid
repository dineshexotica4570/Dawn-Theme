<!-- HTML -->
<section class="blog_section p_y">
  <div class="container">
    <div class="tittle_heading">
      <{{ headingSize }}>{{ heading }}</{{ headingSize }}>
    </div>
    <div class="row" id="blogPostsRow">
      <!-- Blog posts will be dynamically inserted here -->
    </div>
  </div>
</section>

<script>
  // JavaScript for dynamic content integration
  document.addEventListener("DOMContentLoaded", function() {
    fetch('/api/blogPosts') // Assuming this endpoint returns blog data
      .then(response => response.json())
      .then(data => {
        const blogPostsRow = document.getElementById('blogPostsRow');
        data.slice(0, {{ postLimit }}).forEach(post => {
          const column = document.createElement('div');
          column.classList.add('col-md-{{ columnsDesktop }}');
          column.innerHTML = `
            <div class="blog_inner">
              <div class="blog_img">
                <img src="${post.image}" alt="${post.title}">
              </div>
              <div class="blog_content">
                <h6>${post.title}</h6>
                <p>${post.content}</p>
                <a href="#" class="blog_link">Read More</a>
              </div>
            </div>
          `;
          blogPostsRow.appendChild(column);
        });
      })
      .catch(error => console.error('Error fetching blog data:', error));
  });
</script>



{% schema %}
{
  "name": "t:sections.custom-blog.name",
  "tag": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "inline_richtext",
      "id": "heading",
      "default": "Blog posts",
      "label": "t:sections.custom-blog.settings.heading.label"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__1.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h0",
          "label": "t:sections.all.heading_size.options__3.label"
        }
      ],
      "default": "h1",
      "label": "t:sections.all.heading_size.label"
    },
    {
      "type": "blog",
      "id": "blog",
      "label": "t:sections.custom-blog.settings.blog.label"
    },
    {
      "type": "range",
      "id": "post_limit",
      "min": 2,
      "max": 4,
      "step": 1,
      "default": 3,
      "label": "t:sections.custom-blog.settings.post_limit.label"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 4,
      "step": 1,
      "default": 3,
      "label": "t:sections.custom-blog.settings.columns_desktop.label"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "info": "t:sections.all.colors.has_cards_info",
      "default": "scheme-1"
    },
    {
      "type": "checkbox",
      "id": "show_image",
      "default": true,
      "label": "t:sections.custom-blog.settings.show_image.label",
      "info": "t:sections.custom-blog.settings.show_image.info"
    },
    {
      "type": "checkbox",
      "id": "show_date",
      "default": true,
      "label": "t:sections.custom-blog.settings.show_date.label"
    },
    {
      "type": "checkbox",
      "id": "show_author",
      "default": false,
      "label": "t:sections.custom-blog.settings.show_author.label"
    },
    {
      "type": "checkbox",
      "id": "show_view_all",
      "default": true,
      "label": "t:sections.custom-blog.settings.show_view_all.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ],
  "presets": [
    {
      "name": "t:sections.custom-blog.presets.name"
    }
  ]
}
{% endschema %}